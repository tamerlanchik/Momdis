function plotfield2d(i);
load('data_getparopt2d.mat');
load('data_qmm.mat');
load('dat_x0.mat');
Qtemp=Q;
Qnew=[];
Jnew=[];
for j=1:100
    Qtemp(i)=q_min(i)+qmax(i)*j/100;
    Xtemp=mat_int(Qtemp, 0);
    Jtemp=mat_fun(Xtemp, Qtemp,0);
    Qnew=[Qnew; Qtemp];
    Jnew=[Jnew; ]
